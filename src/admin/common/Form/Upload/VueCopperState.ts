export default interface VueCopperState {
  img: string;
  outputType: string;
  canScale: boolean;
  canMove: boolean;
  autoCrop: boolean;
  width: number;
  fixed: number;
  fixedNumber: number;
  fixedBox: string;
  centerBox: string;
}
